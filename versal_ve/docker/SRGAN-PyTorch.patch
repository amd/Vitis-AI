diff --git a/data/bicubic_down_dataset.py b/data/bicubic_down_dataset.py
index 98e24373c49a2e2cc5e50c2f8c08f6538ca6f1aa..ccc2cd9b705282728f5851a3daa329e9643d7404 100644
--- a/data/bicubic_down_dataset.py
+++ b/data/bicubic_down_dataset.py
@@ -8,11 +8,15 @@ import utils.util as util
 
 from PIL import Image
 
+import mipso_custom
+
 class BicubicDownDataset(data.Dataset):
-    def initialize(self, dataset_opt):
+    def initialize(self, dataset_opt, args):
         self.phase = dataset_opt.phase
         self.dir = dataset_opt.dataroot
 
+        self.args = args
+
         if dataset_opt.use_subset:
             subset_path = os.path.join(self.dir, 'subset.txt')
             with open(subset_path) as f:
@@ -28,7 +32,9 @@ class BicubicDownDataset(data.Dataset):
     def __getitem__(self, index):
         path = self.paths[index]
         H_img = self.transform_H(Image.open(path).convert('RGB'))
-        L_img = self.transform_L(H_img)
+
+        H_img = mipso_custom.mipso_resize_input(self.args.inputSize, H_img, lambda img, x, y: img.resize( (x,y) ))
+        L_img = self.transform_L(H_img) if not self.args.no_downscale else H_img
         H_tensor = self.to_tensor(H_img)
         L_tensor = self.to_tensor(L_img)
         out_dict = dict(L=L_tensor, H=H_tensor)
diff --git a/data/datasets.py b/data/datasets.py
index b0c4befc0eaeff86d0caff2f8265df9ff53c8862..15d07cf04404a8f9d907ffa98f2ff65355005ad8 100644
--- a/data/datasets.py
+++ b/data/datasets.py
@@ -1,11 +1,11 @@
 from __future__ import print_function
 
-def create_dataset(dataset_opt):
+def create_dataset(dataset_opt, args):
     if dataset_opt.mode == 'bicubic_down':
         from data.bicubic_down_dataset import BicubicDownDataset
         dataset = BicubicDownDataset()
     else:
         raise NotImplementedError("Dataset [%s] is not recognized." % dataset_opt.mode)
-    dataset.initialize(dataset_opt)
+    dataset.initialize(dataset_opt, args)
     print('Dataset [%s] is created.' % dataset.name())
     return dataset
diff --git a/data/util.py b/data/util.py
index 1d14a668652640fc4a3acac756eab9591886dd02..19062aa0d1ad47b5a2c6d972fb15d2e679bfdd3e 100644
--- a/data/util.py
+++ b/data/util.py
@@ -12,7 +12,7 @@ def is_image_file(filename):
     return any(filename.endswith(extension) for extension in IMG_EXTENSIONS)
 
 def get_image_paths(dir):
-    assert os.path.isdir(dir), '%s is not a valid directory' % dir
+    assert os.path.isdir(os.path.expandvars(dir)), '%s is not a valid directory' % dir
     images = []
     for root, _, fnames in sorted(os.walk(dir)):
         for fname in fnames:
@@ -23,7 +23,7 @@ def get_image_paths(dir):
     return images
 
 def get_image_paths_recursive(dir, images):
-    assert os.path.isdir(dir), '%s is not a valid directory' % dir
+    assert os.path.isdir(os.path.expandvars(dir)), '%s is not a valid directory' % dir
     for root, subdirs, fnames in sorted(os.walk(dir)):
         for fname in fnames:
             if is_image_file(fname):
diff --git a/mipso_custom.py b/mipso_custom.py
new file mode 100644
index 0000000000000000000000000000000000000000..5d059fd930c9c64f572ff8695b08a468b020861a
--- /dev/null
+++ b/mipso_custom.py
@@ -0,0 +1,355 @@
+
+from collections import deque
+from threading import Thread
+import cv2
+import os
+
+import numpy as np
+import PIL
+############################################################
+############################################################
+
+# the application will open several window and place them on the desktop at
+# specific location. The below variables allow to configure the location
+
+# this is the X/Y offset for window position
+WINDOW_X_OFFSET         = int(os.environ.get("WINDOW_X_OFFSET")         or 0)
+WINDOW_Y_OFFSET         = int(os.environ.get("WINDOW_Y_OFFSET")         or 0)
+
+# this is the number of window per row
+WINDOW_PER_ROW          = int(os.environ.get("WINDOW_PER_ROW")          or 3)
+
+# this is a scaling factor to reduce the window dimension (2: window will be 2x smaller)
+WINDOW_RATIO            = float(os.environ.get("WINDOW_RATIO")          or 1)
+
+# this force the width and height of the video
+WINDOW_WIDTH            = int(os.environ.get("WINDOW_WIDTH")            or 0)
+WINDOW_HEIGHT           = int(os.environ.get("WINDOW_HEIGHT")           or 0)
+
+# this is the index position to place a window so a slide can be displayed. For instance, in a 3x3 configuration use 4 to let the center empty
+WINDOW_SLIDE_IDX        = int(os.environ.get("WINDOW_SLIDE_IDX")        or 4)
+
+# this is the number of batch (usually the number of cores) per system
+BATCH_PER_SYSTEM        = int(os.environ.get("BATCH_PER_SYSTEM")        or 1)
+
+# this is the number of system per board
+SYSTEM_PER_BOARD        = int(os.environ.get("SYSTEM_PER_BOARD")        or 2)
+
+# show info/FPS by default
+SHOW_INFO               = int(os.environ.get("SHOW_INFO")               or 0)
+SHOW_FPS                = int(os.environ.get("SHOW_FPS")                or 0)
+
+
+# enable the MJPG mode
+OPENCV_CAMERA_MJPG      = int(os.environ.get("OPENCV_CAMERA_MJPG")      or 1)
+# force camera width/height
+OPENCV_CAMERA_WIDTH     = int(os.environ.get("OPENCV_CAMERA_WIDTH")     or 0)
+OPENCV_CAMERA_HEIGHT    = int(os.environ.get("OPENCV_CAMERA_HEIGHT")    or 0)
+
+
+############################################################
+############################################################
+
+# only for YoloV2:
+def mipso_get_interpolation():
+    return cv2.INTER_NEAREST
+    return cv2.INTER_LINEAR
+    #return cv2.INTER_LANCZOS4
+
+# only for YoloV2
+def mipso_camera_reduce_delay():
+    return False # normal execution: capture / inference / display from capture
+    return True # reduce delay execution: inference on prev capture / capture / display from capture using previous inference
+
+# only for YoloV2
+def mipso_box_rounding(i):
+    return i
+    round = 20
+    return ((i + round) // (2*round) ) * 2*round
+
+def mipso_camera_init(c):
+    print ("Initial camera resolution {}x{} @ {}".format(
+        c.get(cv2.CAP_PROP_FRAME_WIDTH),
+        c.get(cv2.CAP_PROP_FRAME_HEIGHT),
+        c.get(cv2.CAP_PROP_FPS)))
+    if OPENCV_CAMERA_WIDTH != 0:
+        c.set(cv2.CAP_PROP_FRAME_WIDTH , OPENCV_CAMERA_WIDTH)
+    if OPENCV_CAMERA_HEIGHT != 0:
+        c.set(cv2.CAP_PROP_FRAME_HEIGHT , OPENCV_CAMERA_HEIGHT)
+    if OPENCV_CAMERA_MJPG != 0:
+        c.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter.fourcc('M', 'J', 'P', 'G'))
+    print ("Using camera resolution {}x{} @ {}".format(
+        c.get(cv2.CAP_PROP_FRAME_WIDTH),
+        c.get(cv2.CAP_PROP_FRAME_HEIGHT),
+        c.get(cv2.CAP_PROP_FPS)))
+
+def mipso_window_init(idx, width, height):
+    cv2.namedWindow(str(idx), cv2.WINDOW_GUI_NORMAL | cv2.WINDOW_FREERATIO)
+    #cv2.namedWindow(str(idx), cv2.WINDOW_NORMAL)
+
+    if WINDOW_WIDTH != 0 and WINDOW_HEIGHT != 0:
+        cur_width = WINDOW_WIDTH
+        cur_height = WINDOW_HEIGHT
+    else:
+        cur_width = int(width // WINDOW_RATIO)
+        cur_height = int(height // WINDOW_RATIO)
+
+    cv2.resizeWindow(str(idx), (cur_width, cur_height))
+    board=0
+    system=0
+    core=0
+    if 'VAISW_RUNSESSION_ENABLECORES' in os.environ:
+        c = os.environ['VAISW_RUNSESSION_ENABLECORES'].split(':')[0].split('_')
+        if len(c[0]) > 0:
+            board = int(c[0][1:])
+        if len(c) > 1:
+            system = int(c[1][1:])
+        if len(c) > 2:
+            core = int(c[2][1:])
+
+    cur_idx = idx + core + BATCH_PER_SYSTEM * ( system + SYSTEM_PER_BOARD * board)
+    # in case we want to put slide somewhere
+    if cur_idx >= WINDOW_SLIDE_IDX:
+        cur_idx = cur_idx + 1
+    pos_x = WINDOW_X_OFFSET + int( (cur_idx % WINDOW_PER_ROW)*cur_width)
+    pos_y = WINDOW_Y_OFFSET + int( 0 + (cur_idx // WINDOW_PER_ROW)*cur_height)
+
+    print("Move window {} {}x{} for system {} @ ({} , {})".format(idx, cur_width, cur_height, system, pos_x, pos_y))
+    cv2.moveWindow(str(idx), pos_x, pos_y)
+
+def mipso_parse_input(file):
+    mode='file'
+    for part in file.split(':'):
+        if 'camera' == part:
+            mode='camera'
+            continue
+        if part.startswith('http'):
+            mode=part
+            continue
+        if part.isdigit():
+            yield mode, int(part)
+        elif mode.startswith('http'):
+            yield 'http', mode + ':' + part
+            mode='file'
+        else:
+            mode='file'
+            yield mode, part
+
+
+class captureDirectory(object):
+    def __init__(self, path):
+        self.path = path
+        self.l = iter([os.path.join(self.path, x) for x in sorted(os.listdir(self.path))])
+
+    def isOpened(self):
+        return os.path.isdir(self.path)
+    def get(self, prop):
+        # only return the FPS
+        return 1
+    def read(self):
+        try:
+            i = next(self.l)
+        except StopIteration:
+            return False, None
+        return True, cv2.imread(i)
+
+class liveCapture(object):
+    def __init__(self,src):
+        self.stop = False
+        self.status = False
+        self.frames = deque([])
+        self.capture = cv2.VideoCapture(src)
+        #self.capture.set(cv2.CAP_PROP_BUFFERSIZE,2)
+        self.FPS = self.capture.get(cv2.CAP_PROP_FPS)
+        self.thread1 = Thread(target = self.bg_read, args=())
+        self.thread1.daemon = True
+        self.thread1.start()
+        self.readFps = 1
+    def bg_read(self):
+        while not self.stop:
+            if self.capture.isOpened():
+                self.status, frame = self.capture.read()
+                if self.status:
+                    self.frames.append(frame)
+    def isOpened(self):
+        return self.capture.isOpened()
+    def read(self):
+        # do a kind of adaptative fast forward with input buffer of 200 images
+        l = len(self.frames)
+        if l > 200:
+            self.readFps = 1 << ((l//100)-1)
+        elif l < 200:
+            self.readFps = 1
+        if len(self.frames) > 0:
+            f = self.frames[0]
+            for _ in range(int(self.readFps)):
+                # keep always 1 frame in the buffer
+                if len(self.frames) == 1:
+                    break
+                self.frames.popleft()
+            return True, f
+        else:
+            return False, None
+    def get(self, prop):
+        # only return the FPS
+        return int(self.FPS)
+    def __del__(self):
+        self.stop = True
+        self.thread1.join()
+
+
+def mipso_opencv_capture(file):
+    """
+    return a list of opencv capture device
+    """
+    camera = []
+    # TODO: use a better handling of the different mode (class)
+    if 'camera' == file:
+        # open all camera
+        camera_idx=0
+        while True:
+            c = cv2.VideoCapture(camera_idx)
+            if c.isOpened() == False:
+                break
+            camera.append(c)
+            mipso_camera_init(c)
+            camera_idx = camera_idx + 1
+    else:
+        for mode, url in mipso_parse_input(file):
+            if mode.startswith('http') and 'youtube' in url:
+                import pafy
+                option = url.split('@')
+                v = pafy.new(option[0])
+                resolution = [s.resolution for s in v.allstreams]
+                if option[-1] == 'list':
+                    print(f'Available resolution for {option[0]} are:')
+                    print('\n'.join(resolution))
+                    exit(0)
+                elif option[-1] in resolution:
+                    url = v.allstreams[resolution.index(option[-1])].url
+                else:
+                    url = v.getbest().url
+                if 'live' in option:
+                    c = liveCapture(url)
+                else:
+                    c = cv2.VideoCapture(url)
+            elif os.path.isdir(url):
+                c = captureDirectory(url)
+            else:
+                c = cv2.VideoCapture(url)
+            if c.isOpened() == False:
+                print("Error opening source {} {}".format(mode, url))
+                break
+            if mode == 'camera':
+                mipso_camera_init(c)
+            camera.append(c)
+
+    assert len(camera) > 0, 'Cannot capture source'
+    return camera
+
+# TODO: create a Mipso class
+showHelp = False
+pauseMode = False
+showWindowInfo = SHOW_INFO != 0
+showFps = SHOW_FPS != 0
+
+def mipso_process_key(key):
+    global showHelp
+    global pauseMode
+    global showWindowInfo
+    global showFps
+
+    if pauseMode:
+        print ("pause mode, press 'p' to play or any other key to move to the next frame")
+    while True:
+        if key == 27:
+            return key
+        if key > 0 and chr(key) in 'h':
+            showHelp = not showHelp
+        if key > 0 and chr(key) in 'p':
+            pauseMode = not pauseMode
+        if key > 0 and chr(key) in 'i':
+            showWindowInfo = not showWindowInfo
+        if key > 0 and chr(key) in 'f':
+            showFps = not showFps
+        if not pauseMode or key != -1:
+            break
+        key = cv2.waitKey(1)
+    return key
+
+def print_help(frame):
+
+    HELP="\
+Key usage:\n\
+    ESC : exit the demo\n\
+    h   : toggle displaying this help\n\
+    p   : pause the video (any other key will do frame by frame)\n\
+    i   : toggle showing network information\n\
+    f   : toggle showing FPS\n\
+"
+    scale = 1
+    if frame.shape[1] < 1240:
+        scale = scale * frame.shape[1] / 1240
+    for y, line in enumerate(HELP.split('\n')):
+        cv2.putText(frame, line, (20, int(scale*(200 + y * 40))), cv2.FONT_HERSHEY_SIMPLEX,
+                    scale, (255,255,0), 1)
+
+
+def print_text(frame, x, y, text, scale=1.2):
+    font                   = cv2.FONT_HERSHEY_SIMPLEX
+    bottomLeftCornerOfText = (10,500)
+    fontScale              = scale
+    fontColor              = (0,0,255)
+    lineType               = 2 if scale >= 1 else 1
+
+    cv2.putText(frame, text, (x, y),
+        font,
+        fontScale,
+        fontColor,
+        lineType)
+
+def mipso_window_info(f, idx, name, latency):
+    if showHelp:
+        print_help(f)
+    if showWindowInfo:
+        scale = 1
+        if f.shape[1] < 500:
+            scale = scale * f.shape[1] / 500
+        if showFps:
+            print_text( f, 10, int(scale*30), "Vaisw: {} FPS={}".format(name, str(int(1/latency))), scale )
+        else:
+            print_text( f, 10, int(scale*30), "Vaisw: {}".format(name), scale)
+        #print_text(f, 10, 50, "Latency is: " + str(int(1000*latency)) + " ms. FPS is: " + str(int(1/latency)) + " img/s")
+
+#inputSize is a tuple (x, y), meaning (width, height). Flag should respect that format
+#resize_function is a caller-provided function that takes image, x and y as input and returns the resized image
+#get_size_function is a caller-provided function that returns the size (x, y) of the input image to handle cases where the input image is not know
+inputSize = None
+def mipso_resize_input(flag_inputSize, input, resize_function, get_size_function=None):
+    global inputSize
+
+    assert callable(resize_function), 'Expected a resize function as third argument'
+
+    def parse_inputSize(inputSize_string):
+        return tuple( int(dim) for dim in inputSize_string.split('x') )
+
+    if flag_inputSize is None:
+        out_image = input
+    elif flag_inputSize == "auto":
+        if inputSize is None:
+            if isinstance(input, PIL.Image.Image):
+                inputSize = input.size
+            elif isinstance(input, np.ndarray):
+                inputSize = input.shape[1::-1]
+            elif get_size_function is not None:
+                inputSize = get_size_function(input)
+            else:
+                raise TypeError(f'Unknown image type {type(input)}, cannot guess shape of first image')
+            out_image = input
+        else:
+            out_image = resize_function(input, x=inputSize[0], y=inputSize[1])
+    else:
+        if inputSize is None:
+            inputSize = parse_inputSize(flag_inputSize)
+        out_image = resize_function(input, x=inputSize[0], y=inputSize[1])
+    return out_image
diff --git a/models/modules/util.py b/models/modules/util.py
index 6ca7a5a6a705dab0e23c0eb2236b5d709e775bb7..f64bd8a40343a5942c93c0f896780931508edeae 100644
--- a/models/modules/util.py
+++ b/models/modules/util.py
@@ -17,4 +17,4 @@ def save_network(save_dir, network, network_label, iter_label, gpu_ids):
 
 # helper loading function
 def load_network(load_path, network):
-    network.module.load_state_dict(torch.load(load_path),False)
+    network.load_state_dict(torch.load(os.path.expandvars(load_path)),False)
diff --git a/models/networks.py b/models/networks.py
index 0b53714094e382402ecd78e12f82dc4cf5674c83..b0d357c79a67b7297519de06b017fed94ac1baea 100644
--- a/models/networks.py
+++ b/models/networks.py
@@ -18,7 +18,7 @@ def define_G(opt):
     else:
         raise NotImplementedError('Generator model [%s] is not recognized' % which_model)
 
-    netG = nn.DataParallel(netG, device_ids=gpu_ids)
+    #netG = nn.DataParallel(netG, device_ids=gpu_ids)
     return netG
 
 
diff --git a/models/sr_resnet_test_model.py b/models/sr_resnet_test_model.py
index 361dcc86118527d3e8b4271bf2402ac59537e67e..f5b59e43d402856647e94568058bb70cfd3ad4fe 100644
--- a/models/sr_resnet_test_model.py
+++ b/models/sr_resnet_test_model.py
@@ -43,9 +43,9 @@ class SRResNetTestModel(BaseModel):
 
     def get_current_visuals(self):
         out_dict = OrderedDict()
-        out_dict['low-resolution'] = self.real_L.data[0]
-        out_dict['super-resolution'] = self.fake_H.data[0]
-        out_dict['ground-truth'] = self.real_H.data[0]
+        out_dict['low-resolution'] = self.real_L.data
+        out_dict['super-resolution'] = self.fake_H.data
+        out_dict['ground-truth'] = self.real_H.data
         return out_dict
 
     def load(self):
diff --git a/options/options.py b/options/options.py
index 7fdcc5771593ebd4e2bad7cad7fc7cab7556e1b2..ab049df9afe1a3d3edc5c17f1e88cfe038ffa9ce 100644
--- a/options/options.py
+++ b/options/options.py
@@ -11,13 +11,13 @@ Read the config file and parse it to a option object.
 def get_timestamp():
     return datetime.now().strftime('%y%m%d-%H%M%S')
 
-def parse(opt_path, is_train=True):
+def parse(opt_path, is_train=True, args=None):
     with open(opt_path, 'r') as f:
         opt = json.load(f, object_pairs_hook=OrderedDict)
     opt['is_train'] = is_train
 
     for key, path in opt['path'].items():
-        opt['path'][key] = os.path.expanduser(path)
+        opt['path'][key] = os.path.expandvars(path)
     if is_train:
         experiments_root = os.path.join(opt['path']['root'], 'experiments', opt['name'])
         opt['path']['experiments_root'] = experiments_root
@@ -29,9 +29,15 @@ def parse(opt_path, is_train=True):
         opt['path']['results_root'] = results_root
         opt['path']['log'] = os.path.join(results_root, 'log')
         opt['path']['test_images'] = os.path.join(results_root, 'test_images')
+        if args:
+            opt['path']['pretrain_model_G'] = args.model_path
 
     for dataset in opt['datasets']:
-        dataset['dataroot'] = os.path.expanduser(dataset['dataroot'])
+        dataset['dataroot'] = os.path.expandvars(dataset['dataroot'])
+        if args:
+            dataset['dataroot'] = args.input
+            dataset['name'] = args.input
+
     return opt
 
 def save(opt):
diff --git a/options/test/custom.json b/options/test/custom.json
new file mode 100644
index 0000000000000000000000000000000000000000..33349e4849d53ae142c3389100417cf8ac22e26c
--- /dev/null
+++ b/options/test/custom.json
@@ -0,0 +1,33 @@
+{
+  "name": "Custom_user_run",
+  "model": "sr_resnet_test",
+
+  "datasets": [{
+    "name": "OVERRIDDEN_BY_ARGS",
+    "mode": "bicubic_down",
+    "phase": "test",
+    "dataroot": "OVERRIDDEN_BY_ARGS",
+
+    "transform_H": {
+      "modcrop_size": 4
+    },
+    "transform_L": {
+      "downscale": 4
+    }
+  }],
+
+  "path": {
+    "root": "./",
+    "pretrain_model_G": "OVERRIDEN_BY_ARGS"
+  },
+
+  "network": {
+    "norm_type": "batch",
+
+    "which_model_G": "sr_resnet",
+    "ngf": 64,
+    "ngb": 16,
+    "input_ngc": 3,
+    "output_ngc": 3
+  }
+}
diff --git a/run b/run
new file mode 100755
index 0000000000000000000000000000000000000000..5871feef194d14688743d24b9ae02d75739e3f6d
--- /dev/null
+++ b/run
@@ -0,0 +1,19 @@
+#!/usr/bin/env bash
+
+input=$1
+shift
+
+model_path="weights/SRResNet/latest_G.pth"
+
+while [ $# -gt 0 ]
+do
+  if [ "$1" = "--srgan" ]
+  then
+   model_path="weights/SRGAN/latest_G.pth"
+  else
+   args="$args $1"
+  fi
+  shift
+done
+
+python3 ./test.py --input $input --option options/test/custom.json --inputSize auto --model_path $model_path $args
diff --git a/test.py b/test.py
index e89be10b211a01be76ea604ff83bd5165a351363..770f1cdb547ba8e43eb3e00595a58e8d4c63c66d 100644
--- a/test.py
+++ b/test.py
@@ -13,37 +13,92 @@ import utils.util as util
 import utils.metric as metric
 import utils.convert as convert
 from data.transforms import Scale
+import data.transforms as DT
 from torchvision.transforms import ToTensor
 from PIL import Image
 
+import cv2
+
+import mipso_custom
+
 parser = argparse.ArgumentParser()
 parser.add_argument('--options', type=str, required=True, help='Path to options JSON file.')
+### VAISW ADDED ARGS ###
+parser.add_argument('--input', type=str, required=False, default=False, help='Path to video')
+parser.add_argument('--inputSize', type=str, required=False, help='size to resize input(s)')
+parser.add_argument('--loop', action='store_true', required=False, default=False, help='Loop over video')
+parser.add_argument('--out_file', type=str, default=None ,required=False, help='outpath path for images/video')
+parser.add_argument('--as_images', action='store_true', help='save video as images')
+parser.add_argument('--batch', type=int, required=False, default=1, help='Batch size')
+parser.add_argument('--model_path', type=str, required=True, help='')
+parser.add_argument('--no_downscale', action='store_true', default=False, help='avoid downscaling of input image')
+########################
 args = parser.parse_args()
 options_path = args.options
-opt = options.parse(options_path, is_train=False)
+opt = options.parse(options_path, is_train=False, args=args)
 util.mkdirs((path for key , path in opt['path'].items() if not key == 'pretrain_model_G')) # Make all directories needed
 opt = options.dict2box(opt)
 
+if os.path.isdir(args.input):
+    video=False
+else:
+    video=args.input
+
 
 from data.datasets import create_dataset
 from data.data_loader import create_dataloader
 from models.models import create_model
 
+def videoIterator(video):
+    while True:
+        success, frame = video.read()
+        if success:
+            frame = mipso_custom.mipso_resize_input(args.inputSize, frame, lambda img, x, y : cv2.resize(img , (x, y), interpolation = cv2.INTER_AREA))
+            gt = Image.fromarray(frame)
+            lr = DT.get_transform_L(opt.datasets[0].transform_L)(gt) if not args.no_downscale else gt
+            gt = ToTensor()(gt).unsqueeze_(0)
+            lr = ToTensor()(lr).unsqueeze_(0)
+            name = "Frame-"+str(int(video.get(cv2.CAP_PROP_POS_FRAMES)))
+            yield { 'H': gt, 'L': lr, 'path': [name] }
+        elif args.loop:
+            video.set(cv2.CAP_PROP_POS_FRAMES, 0)
+            print('Last frame reached, no loop option, quitting that video')
+        else:
+            break
+
 # Create test dataset and dataloader
 test_loaders = []
 test_set_names = []
-for dataset_opt in opt.datasets:
-    test_set = create_dataset(dataset_opt)
-    test_loader = create_dataloader(test_set, dataset_opt)
-    test_size = len(test_set)
-    test_set_name = dataset_opt.name
-    print('Number of test images in [%s]: %d' % (test_set_name, test_size))
-    test_loaders.append(test_loader)
-    test_set_names.append(test_set_name)
+test_sizes = []
+
+if video:
+    vidcaps = mipso_custom.mipso_opencv_capture(video)
+    for vid in vidcaps:
+        test_loaders.append(videoIterator(vid))
+        test_set_names.append("User provided content")
+        test_size = int(vid.get(cv2.CAP_PROP_FRAME_COUNT))
+        test_sizes.append(test_size)
+        fps = int(vid.get(cv2.CAP_PROP_FPS))
+else:
+    for dataset_opt in opt.datasets:
+        test_set = create_dataset(dataset_opt, args)
+        test_loader = create_dataloader(test_set, dataset_opt)
+        test_size = len(test_set)
+        test_set_name = os.path.expandvars(dataset_opt.name)
+        print('Number of test images in [%s]: %d' % (test_set_name, test_size))
+        test_loaders.append(test_loader)
+        test_set_names.append(test_set_name)
+        test_sizes.append(test_size)
 
 # Create model
 model = create_model(opt)
 
+# Mipso video vars
+inf_name=args.model_path.split(os.path.sep)[1]
+firstShow = True
+out_images = []
+fourcc = cv2.VideoWriter_fourcc(*'XVID')
+
 # Path for log file
 test_log_path = os.path.join(opt.path.log, 'test_log.txt')
 if os.path.exists(test_log_path):
@@ -52,64 +107,123 @@ if os.path.exists(test_log_path):
 
 print('Start Testing ...')
 
-for test_set_name, test_loader in zip(test_set_names, test_loaders):
+for test_set_name, test_loader, test_size in zip(test_set_names, test_loaders, test_sizes):
     print('Testing [%s]...' % test_set_name)
     test_start_time = time.time()
     test_results = OrderedDict()
     test_results['psnr'] = []
     test_results['ssim'] = []
+    image_id = 0
 
     log_str = '\nTest set: [%s]\n' % test_set_name
-    print(log_str)
+    batch_data = None
     for i, data in enumerate(test_loader):
-
-        model.feed_data(data, volatile=True)
-        img_path = data['path'][0]
-        img_name = os.path.splitext(os.path.basename(img_path))[0]
-        img_dir = os.path.join(opt.path.test_images, test_set_name, img_name)
-        util.mkdir(img_dir)
-
+        if batch_data is None:
+            batch_data = data
+        else:
+            batch_data['H'] = torch.cat( (batch_data['H'], data['H']), 0)
+            batch_data['L'] = torch.cat( (batch_data['L'], data['L']), 0)
+            batch_data['path'].append(*data['path'])
+
+        if batch_data['H'].shape[0] != args.batch and i != (test_size-1):
+            continue
+
+        model.feed_data(batch_data, volatile=True)
+        inf_start = time.time()
         model.test() # test
+        took_inf = time.time() - inf_start
         visuals = model.get_current_visuals()
 
-        sr_img = convert.tensor2img_np(visuals['super-resolution']) # uint8
-        gt_img = convert.tensor2img_np(visuals['ground-truth']) # uint8
-        cropped_sr_img = sr_img[4:-4, 4:-4, :]
-        cropped_gt_img = gt_img[4:-4, 4:-4, :]
-
-        # Save SR images for reference
-        save_img_path = os.path.join(img_dir, 'sr.png')
-        util.save_img_np(sr_img, save_img_path)
-
-        # Convert images to luma space
-        cropped_sr_img = convert.rgb2y(cropped_sr_img)
-        cropped_gt_img = convert.rgb2y(cropped_gt_img)
-
-        # Calculate quantitative performance metric
-        psnr = metric.psnr(cropped_sr_img, cropped_gt_img)
-        ssim = metric.ssim(cropped_sr_img, cropped_gt_img)
-
-        test_results['psnr'].append(psnr)
-        test_results['ssim'].append(ssim)
-
-        tmp_str = '\nImage [%s]\n' % (img_path)
-        for label, values in test_results.items():
-            tmp_str += '%s: %.4f \n' % (label, values[i])
-        log_str += tmp_str
-        print(tmp_str)
+        for batch_image_id in range(batch_data['H'].shape[0]):
+            sr_img = convert.tensor2img_np(visuals['super-resolution'][batch_image_id]).copy() # uint8
+            gt_img = convert.tensor2img_np(visuals['ground-truth'][batch_image_id]) # uint8
+            lr_img = convert.tensor2img_np(visuals['low-resolution'][batch_image_id]) # uint8
+
+            if video:
+                mipso_custom.mipso_window_info(sr_img, 0, inf_name, took_inf)
+                if args.out_file:
+                    if args.out_file != "/dev/null":
+                        if args.as_images:
+                            cv2.imwrite(args.out_file + str(i) +".png", sr_img)
+                        else:
+                            out_images.append(sr_img)
+                else:
+                    if firstShow:
+                        height, width, _ = sr_img.shape
+                        mipso_custom.mipso_window_init(0, width, height)
+                    cv2.imshow(str(0), sr_img)
+
+            cropped_sr_img = sr_img[4:-4, 4:-4, :]
+            cropped_gt_img = gt_img[4:-4, 4:-4, :]
+
+            img_path = batch_data['path'][batch_image_id]
+            img_name = os.path.splitext(os.path.basename(img_path))[0]
+            img_dir = os.path.join(opt.path.test_images, test_set_name, img_name)
+
+            if not video and args.out_file is None:
+                util.mkdir(img_dir)
+
+                # Save SR images for reference
+                save_img_path = os.path.join(img_dir, str(image_id)+'-sr.png')
+                util.save_img_np(sr_img, save_img_path)
+
+                # Save GT images for reference
+                save_gt_path = os.path.join(img_dir, str(image_id)+'-gt.png')
+                util.save_img_np(gt_img, save_gt_path)
+
+                # Save LR imges for reference
+                save_lr_path = os.path.join(img_dir, str(image_id)+'-lr.png')
+                util.save_img_np(lr_img, save_lr_path)
+
+            #If we don't downscale, dont compute performance metric
+            if not args.no_downscale:
+                # Convert images to luma space
+                cropped_sr_img = convert.rgb2y(cropped_sr_img)
+                cropped_gt_img = convert.rgb2y(cropped_gt_img)
+
+                # Calculate quantitative performance metric
+                psnr = metric.psnr(cropped_sr_img, cropped_gt_img)
+                ssim = metric.ssim(cropped_sr_img, cropped_gt_img)
+
+                test_results['psnr'].append(psnr)
+                test_results['ssim'].append(ssim)
+
+                tmp_str = '\nImage [%s]\n' % (img_path)
+                for label, values in test_results.items():
+                    tmp_str += '%s: %.4f \n' % (label, values[image_id])
+                log_str += tmp_str
+                print(tmp_str)
+            image_id += 1
+
+        choice = cv2.waitKey(1)
+        choice = mipso_custom.mipso_process_key(choice)
+        if choice == 27:
+            break
+        firstShow = False
+        batch_data = None
 
     test_duration = time.time() - test_start_time
-
+    if args.out_file and not args.as_images and not args.out_file == '/dev/null':
+        vidwri = cv2.VideoWriter(
+                                args.out_file,
+                                fourcc,
+                                fps, ( (out_images[0].shape[1], out_images[0].shape[0]) ))
+        for img in out_images:
+            vidwri.write(img)
+        vidwri.release()
     print('Time taken: %.3f' % test_duration)
     # Calculate Average
     tmp_str = '\n ------------Overall results------------'
     log_str += tmp_str + '\n'
     print(tmp_str)
-    for label, values in test_results.items():
-        avg_value = sum(values) / len(values)
-        tmp_str =  'avg %s: %.4f \n' % (label, avg_value)
-        log_str += tmp_str
-        print(tmp_str)
+    if not args.no_downscale:
+        for label, values in test_results.items():
+            avg_value = sum(values) / len(values)
+            tmp_str =  'avg %s: %.4f \n' % (label, avg_value)
+            log_str += tmp_str
+            print(tmp_str)
+    else:
+        print("Metrics aren't available without downscale")
     # Write in log file
     with open(test_log_path, "a") as log_file:
         log_file.write('%s' % log_str)
diff --git a/utils/metric.py b/utils/metric.py
index 17b274a62a51faefddab562a493db1b844f20a7b..64c295de7d77b924cc6ce85427afa1da39366f4d 100644
--- a/utils/metric.py
+++ b/utils/metric.py
@@ -1,6 +1,6 @@
 import numpy as np
 import math
-from skimage.measure import compare_ssim
+from skimage.metrics import structural_similarity as compare_ssim
 
 
 """
